

# 🔥 F-SURVIVAL (PROJECT APP TRỌ & AI)

## 🛠 CÀI ĐẶT (Chạy lệnh này ngay khi tải về)
1. **Lấy code:** `git clone https://github.com/ElysiaTheElysier/F-Survival.git`
2. **Cài thư viện:**
   ```bash
   pip install fastapi uvicorn pyodbc langchain faiss-cpu sentence-transformers



## 🚀 CÁCH KẾT NỐI SERVER

* **Leader (Server):** Chạy 2 lệnh:
1. `python main.py` (Bật API)
2. `ngrok http 8000` (Bật Online)


* **Thành viên (Client):**
* Vào Zalo lấy **Link Ngrok** (Dạng `https://xyz.ngrok-free.app`).
* Gán link đó vào biến `API_URL` trong code App. **Cấm dùng localhost.**



##  QUY ĐỊNH NỘP BÀI

1. **Code (Frontend/AI):**
* Đầu giờ: `git pull` (Lấy code mới).
* Cuối giờ: `git push` (Nộp code lên).
* **CẤM:** Không dùng lệnh `-f` (Force push).


2. **Dữ liệu (Data/BA):**
* Excel/Word nộp thẳng vào **Google Drive** (Folder `02_Du_Lieu`).


## 📞 HỖ TRỢ


* **Lỗi Code:** Chụp ảnh màn hình + Copy lỗi ném vào nhóm Zalo.

